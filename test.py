SELECT 
    TYPE,
    MAX(DATE_CREATED) AS DATE_CREATED,
    ID,
    CLIENT_FIO,
    CLIENT_DID,
    IBSO_ID_OR_CALLED_FROM_NUM,
    MAX(CARD_TYPE) KEEP (DENSE_RANK LAST ORDER BY DATE_CREATED) AS CARD_TYPE,
    MAX(HAVE_REDIRECT) AS HAVE_REDIRECT,
    MAX(MONEY) AS MONEY
FROM your_table
GROUP BY TYPE, ID, CLIENT_FIO, CLIENT_DID, IBSO_ID_OR_CALLED_FROM_NUM;
