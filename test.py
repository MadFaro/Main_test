CREATE TABLE analytics.tolog_shop_users_bd AS
SELECT DISTINCT UPPER(a."Email") AS EMAIL, b.BIRTH_DATE  
FROM USB_FNS_LOADER.KO_AD_BK_NIGHT a
LEFT JOIN REF.KO_AD_STORY b 
    ON CAST(a."NetName" AS VARCHAR2(200)) = CAST(b.NETNAME AS VARCHAR2(200))
WHERE a."DEP_ID_L3" = '40050455' 
  AND a."StateNP" = 1 
  AND a."StateAD" = 1 
  AND NOT EXISTS (
      SELECT 1 
      FROM ANALYTICS.TOLOG_SHOP_USERS u
      WHERE UPPER(a."Email") = u.EMAIL
  );
