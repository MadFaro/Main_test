CREATE TABLE users (
    [index]    INTEGER  PRIMARY KEY AUTOINCREMENT,
    pass       TEXT,
    sdep       TEXT,
    login      TEXT,
    fio        TEXT,
    status     INTEGER,
    gender     TEXT,
    birth_date DATETIME
);
