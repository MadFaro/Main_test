LAST_VALUE(GROUP_NAME) OVER (
            PARTITION BY EVALUATEE_NAME 
            ORDER BY LOCAL_START_TIME 
            ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING
        ) AS DESCRIPTION,
